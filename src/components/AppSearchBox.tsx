import {View, Text, StyleSheet} from 'react-native';
// import AppThemeStyles from '../utilities/styles';

const AppSearchBox = (): JSX.Element => {
  return (
    <View>
      <Text>AppSearchBox</Text>
    </View>
  );
};

export default AppSearchBox;
